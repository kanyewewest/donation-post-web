import { PageTranslateDirective } from './page-translate.directive';

describe('PageTranslateDirective', () => {
  it('should create an instance', () => {
    const directive = new PageTranslateDirective();
    expect(directive).toBeTruthy();
  });
});
